const path = require('path');
const express = require('express');

const eventsController = require('../controllers/event');

const router = express.Router();

reouter.get('/all-events',)

module.exports = router;

//put -> update
//post -> add